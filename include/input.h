#ifndef INPUT_H
#define INPUT_H

// Gestion du clavier pour l'emulateur CHIP-8
// Mapping QWERTY vers le keypad hexadecimal CHIP-8

// Met a jour l'etat du keypad CHIP-8 depuis le clavier
void update_keypad(void);

#endif
